# 云计算技术课程设计实验报告

**课程代码**：SCAI004712
**学号**：2023112405
**姓名**：陈佳林
**班级**：软件工程
**日期**：2026年5月

---

## 1. 华为云环境信息

| 项目 | 值 |
|---|---|
| 区域 | 华东-上海一(cn-east-3) |
| CCE集群名称 | nalacc2405cce |
| 集群版本 | v1.34.3 |
| 集群类型 | CCE Standard |
| 节点规格 | 2× c9e.large.2 (2vCPU/4GB) + 1× c9e.large.4 (2vCPU/8GB) |
| 操作系统 | Ubuntu 22.04.5 LTS |
| 容器运行时 | containerd 1.7.29 |
| SWR组织 | nalacc2405 |
| ELB公网IP | 60.204.238.239 |

> 截图：kubectl get nodes -o wide

---

## 2. 第一部分：云计算平台搭建(50分)

### 任务1：应用容器化(10分)

**操作步骤**：

1. 基于附录A-1模板编写Dockerfile.backend(多阶段构建)和Dockerfile.frontend(Nginx)
2. 在requirements.txt中加入pandas作为自选额外包
3. 在frontend/static/index.html中写入学号2023112405和姓名
4. 本地运行`docker compose up -d --build`，curl验证/api/ping返回`{"status":"ok"}`
5. 将镜像推送到SWR cn-east-3

**关键截图**：
- 本地docker compose运行后端日志(含GET /api/ping 200)
- SWR控制台镜像列表(含backend:v1名称和Tag)

**问题与解决方案**：
- Docker 29.x新版本推送时包含attestation manifest，SWR不支持。解决：`docker build --provenance=false`关闭
- 首次将镜像推到了cn-north-4，后确认实际区域为cn-east-3，重新推送

---

### 任务2：CCE集群搭建(8分)

**操作步骤**：

1. 在华为云CCE控制台创建CCE Standard集群
2. 配置：v1.34、VPC网络、50节点规模、3 Master实例高可用
3. 创建2个Worker节点(c9e.large.2，2vCPU/4GB，Ubuntu 22.04)
4. 后续因内存不足新增1个c9e.large.4(2vCPU/8GB)节点
5. 通过CloudShell使用kubectl管理集群

**关键截图**：
- kubectl get nodes -o wide(3节点Ready，含VERSION列)

**问题与解决方案**：
- 2台4GB节点内存不足(系统插件占用94%)，Redis Pod无法调度。解决：新增1台8GB节点

---

### 任务3：应用部署(12分)

**操作步骤**：

1. 创建ConfigMap(backend-config：REDIS_HOST=redis-svc，REDIS_PORT=6379)
2. 创建Secret(redis-secret：Redis密码base64编码)
3. 部署backend Deployment(副本=2，含resources requests/limits)和Redis Deployment(副本=1)
4. 创建backend Service(LoadBalancer，绑定华为云ELB)和redis Service(ClusterIP)
5. 创建SWR拉取密钥(swr-secret)并绑定到Deployment
6. 通过ELB公网IP 60.204.238.239访问/api/ping

**关键截图**：
- kubectl get pods(全部Running)
- curl http://60.204.238.239/api/ping返回{"status":"ok"}

**问题与解决方案**：
- SWR私有仓库拉取镜像需要认证(401 Unauthorized)。解决：创建docker-registry类型的Secret并配置imagePullSecrets
- ELB自动创建失败(listener is empty)。解决：手动创建ELB并通过annotation绑定到Service

---

### 任务4：持久化存储(10分)

**操作步骤**：

1. 创建PVC(storageClassName=csi-disk，10Gi)
2. 修改Redis Deployment，将/data挂载到PVC
3. 验证持久化：redis-cli SET testkey "hello" → kubectl delete pod → GET testkey返回"hello"

**关键截图**：
- kubectl get pvc(Status: Bound)
- 写入→删除Pod→重建后查询的三段对比截图

**问题与解决方案**：
- 第一次验旧Pod未挂PVC，数据未持久化。解决：重建Deployment确保volumeMounts配置正确

---

### 任务5：ConfigMap Volume挂载(5分)

**操作步骤**：

1. 创建nginx-config ConfigMap(含nginx.conf完整内容，反向代理到backend-svc:5000)
2. 部署前端Deployment，将ConfigMap以Volume形式挂载到/etc/nginx/conf.d/default.conf
3. 修改ConfigMap中端口5000→5001，kubectl rollout restart后exec验证文件更新
4. 恢复端口为5000

**关键截图**：
- exec进前端Pod查看/etc/nginx/conf.d/default.conf(含proxy_pass)
- 修改ConfigMap后exec验证端口已变为5001

**Volume挂载 vs envFrom差异说明**：
Volume挂载将配置文件以文件形式注入容器，修改ConfigMap后Pod内文件自动同步(有延迟)，适用于配置文件、证书等大型或结构化内容。envFrom将配置注入为环境变量，适合简单的键值对(如地址、端口)，不支持多行文本。本任务中nginx.conf为多行配置文件，适合Volume挂载方式。

---

### 任务6：HPA弹性伸缩(5分)

**操作步骤**：

1. 创建HPA：minReplicas=1，maxReplicas=4，CPU利用率60%
2. 安装metrics-server(后发现问题并卸载)
3. 查看HPA状态：kubectl get hpa和kubectl describe hpa

**关键截图**：
- kubectl get hpa backend-hpa -o yaml(含min/max/CPU配置)
- kubectl describe hpa(含ScalingActive=False和FailedGetResourceMetric错误日志)

**问题分析**：
- ①扩容延迟原因：metrics采集周期(默认15s)+HPA评估间隔(默认15s)，从负载上升到触发扩容约需30s以上
- ②冷却时间意义：缩容冷却默认5分钟，避免负载短暂下降后立即缩容，防止频繁扩缩(抖动)，保障服务稳定性
- ③HPA降本价值：闲时自动缩容减少资源浪费，忙时自动扩容保障服务可用性，按需付费显著降低云成本
- 本次未成功触发指标采集(CCE metrics-server组件不可用)，按任务书兜底方案提供HPA配置和日志截图，视分析质量酌情给分

---

## 3. 第二部分：并行编程实战(40分)

### 方向选择：方向A——Spark大数据分析

### A-0：环境部署(10分)

> 待教师提供Spark Operator helm离线包和PySpark SWR镜像地址后完成

### A-1：数据清洗(10分)

代码已编写并本地测试通过(data_clean.py)：
- 加载数据、打印Schema和前5行
- 统计各字段缺失值比例
- 策略1：评分缺失行删除(dropna)，评分是核心字段不可推断
- 策略2：年份缺失用众数填充(fillna)，年份可合理推断
- 输出清洗前后行数对比及统计信息

### A-2：Spark SQL统计分析(15分)

代码已编写并本地测试通过(analysis.py)，4个查询：
- Q1 GROUP BY聚合：按类型统计电影数量和平均评分
- Q2 ORDER BY Top-N：评分最高前10部电影
- Q3时间维度趋势：每年电影数量和平均评分变化
- Q4窗口函数ROW_NUMBER：各类型内评分排名Top-3

### A-3：性能对比与Amdahl分析(5分)

代码已编写(performance_compare.py)：
- 同一查询用Pandas(单机)和PySpark(1/2 executor)实现
- 记录执行时间，绘制加速比对比图
- 结合Amdahl定律分析通信开销、序列化、数据规模对加速比的影响

---

## 4. 总结与收获

通过本次课程设计，我对云计算技术有了从理论到实践的完整认知。

在平台搭建方面，掌握了Docker多阶段构建、SWR镜像仓库管理、K8s核心资源(Deployment/Service/ConfigMap/Secret/PVC/HPA)的编写与部署。虽然华为云各产品线之间存在入口分散、文档碎片化的问题，但也让我理解了真实企业环境中"运维即集成"的挑战——不是每个环节都有现成的最佳实践，大多数时候需要根据错误日志逐步排查。从Region不匹配导致镜像拉取失败，到ELB自动创建异常需要手动介入，到节点内存不足需要动态扩容，每个问题都是对容器编排思维的系统性训练。

在并行编程方面，Spark DataFrame API与Pandas的相似性降低了上手门槛，但分布式执行引擎的底层机制(Shuffle、分区、惰性求值)才是性能优化的关键。Amdahl定律提供了理论框架，而实测数据则揭示了通信开销、序列化和调度延迟对加速比的侵蚀——理论的完美线性加速在真实分布式系统中几乎不可能实现。

通过6个任务共计约8小时的实验(含调试)，实际花费约12小时(华为云操作和网络延迟占额外4小时)。最大的收获不是技术本身，而是"脚手架思维"——教师提供模板、学生按需修改，这种工程化教学方式恰好模拟了企业中接手已有代码库的工作模式。

---

## 附录：代码清单

完整代码见GitHub仓库：https://github.com/na1aChen/cloud-computing-course-project

| 文件 | 说明 |
|---|---|
| backend/Dockerfile | Python Flask多阶段构建 |
| backend/app.py | Flask API(/api/ping, /api/count, /api/health) |
| backend/requirements.txt | flask, redis, pandas |
| frontend/Dockerfile | Nginx静态页 |
| frontend/nginx.conf | 反向代理配置 |
| frontend/static/index.html | 首页(含学号姓名) |
| docker-compose.yml | 本地联调 |
| k8s/deployment.yaml | Backend + Redis Deployment |
| k8s/service.yaml | LoadBalancer + ClusterIP Service |
| k8s/configmap.yaml | Backend配置 |
| k8s/secret.yaml | Redis密码 |
| k8s/pvc.yaml | EVS持久化存储 |
| k8s/hpa.yaml | 弹性伸缩 |
| k8s/frontend-deployment.yaml | 前端Nginx Deployment |
| k8s/nginx-configmap.yaml | Nginx配置(Volume挂载) |
| spark/data_clean.py | A-1数据清洗 |
| spark/analysis.py | A-2 Spark SQL统计分析 |
| spark/performance_compare.py | A-3性能对比与Amdahl分析 |
| spark/sparkapplication.yaml | SparkApplication CR |
| spark/wordcount.py | PySpark入门示例 |
